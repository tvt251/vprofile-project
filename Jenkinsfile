pipeline {
    agent any
    
    stages {
        stage (Test) {
            steps {
                sh 'echo test github trigger'
                sh 'echo test github trigger form vscode'
            }
        }
    }
}
